# 이미지 → ASCII 아트 웹앱 계획

## 1. 핵심 기능 범위 (MVP)

- 순수 HTML·CSS·JavaScript를 `index.html` 하나에 작성한다. 외부 라이브러리, 폰트, 네트워크 요청, 서버, 빌드 과정은 없다.
- 파일을 브라우저에서 직접 열고 PNG·JPEG·WebP 이미지 한 장을 선택하면 자동 변환한다. 이미지는 브라우저 안에서만 처리한다.
- 원본 미리보기와 ASCII 결과를 보여주고, 옵션 변경 시 다시 변환한다.
- 결과를 일반 텍스트로 복사하거나 `.txt` 파일로 내려받는다.
- 지원하지 않거나 손상된 이미지는 짧은 오류 메시지를 표시한다. 결과 생성 전에는 복사·다운로드 버튼을 비활성화한다.
- 드래그 앤 드롭, 다중 파일, GIF 애니메이션, 계정, 저장 이력, 이미지 형태의 결과 내보내기는 제외한다.

## 2. UI 레이아웃 (요소 구성)

위에서 아래로 다음 요소를 배치한다.

1. **제목과 안내:** “이미지를 ASCII 아트로”, “이미지는 서버로 전송되지 않습니다.”
2. **파일 선택:** 기본 파일 입력과 파일명, 처리 상태·오류 메시지.
3. **작은 원본 미리보기:** 이미지가 선택된 뒤 표시한다.
4. **옵션 한 줄:** 문자셋 선택, 컬러 체크박스, 출력 폭 슬라이더와 현재 열 수. 좁은 화면에서는 줄바꿈한다.
5. **결과 영역:** 흰 배경의 `pre` 요소에 고정폭 시스템 글꼴로 표시한다. 공백과 줄바꿈을 보존하고, 자동 줄바꿈 대신 가로 스크롤을 제공한다.
6. **결과 아래 버튼:** “텍스트 복사”, “TXT 다운로드”. 컬러 선택 시에도 두 기능은 색상 없는 텍스트를 내보낸다고 안내한다.

기본 입력·버튼을 사용하고 각 옵션에 레이블을 연결한다. 상태 메시지는 보조 기술에서도 알 수 있게 한다. 복사 실패 시 결과를 직접 선택해 복사할 수 있도록 안내한다.

## 3. 이미지 → ASCII 변환 알고리즘 개요

Canvas API는 브라우저에서 이미지를 그리거나 픽셀 색상을 읽는 기본 기능이다. 변환용 Canvas는 화면에 표시하지 않는다.

1. **이미지 읽기:** 선택한 파일을 객체 URL로 이미지 요소에 로드한다. 로드 실패를 처리하고, 교체하거나 사용이 끝난 URL은 해제한다. 연속으로 파일을 선택하면 최신 선택만 결과에 반영한다.
2. **출력 크기 결정:** 출력 폭을 문자 열 수 C로 사용한다. 원본 크기가 W × H이고 문자 셀의 가로·세로 비율이 r이면 행 수는 `H / W × C × r`을 반올림한 값으로 정한다. 최솟값은 1이다. 고정폭 글꼴의 문자 폭을 측정하고 지정한 줄 높이로 나눠 r을 구해 세로로 늘어지는 현상을 줄인다.
3. **작게 그리기:** Canvas 크기를 출력 열·행 수로 설정하고, 흰색으로 채운 뒤 원본 전체를 축소해 그린다. 투명 영역은 흰 배경으로 합성한다. 브라우저의 이미지 보간을 사용하고 별도의 픽셀 블록 평균 알고리즘은 만들지 않는다.
4. **픽셀 읽기:** 축소한 Canvas에서 `getImageData`로 RGB 값을 한 번 읽는다. Canvas의 픽셀 하나가 출력 문자 하나에 대응한다.
5. **밝기 계산:** 간단한 가중합 `0.2126R + 0.7152G + 0.0722B`로 0~255 밝기를 구한다. 감마 보정·디더링 없이 재미용 변환에 충분한 수준으로 시작한다.
6. **문자 매핑:** 문자셋을 조밀한 문자부터 공백까지 배열한다. 밝기를 문자셋의 인덱스 범위로 비례 변환하고 반올림한다. 흰 배경이므로 어두운 픽셀은 `@`처럼 조밀한 문자, 밝은 픽셀은 공백이 된다.
7. **결과 조립:** 각 행의 문자를 이어 붙이고 행 사이에 줄바꿈을 넣는다. 흑백은 검은 글자의 일반 텍스트로, 컬러는 같은 문자에 해당 픽셀의 RGB 색상을 적용해 표시한다. 문자는 HTML로 해석하지 않고 텍스트로 삽입한다. 복사·다운로드용 일반 텍스트도 함께 유지한다.

출력은 폭 40~160열, 최대 200행으로 제한한다. 행 제한을 넘으면 열 수도 같은 비율로 줄여 종횡비를 유지하고 실제 크기를 표시한다. 최대 약 32,000문자이므로 우선 메인 스레드에서 처리하고 Worker는 도입하지 않는다. 원본 이미지 디코딩 비용은 별개이므로 입력은 10MB 이하로 제한하고 실패를 안내한다.

## 4. 사용자 옵션 — 꼭 필요한 것만

| 요소 | 기본값·범위 | 동작 |
| --- | --- | --- |
| 문자셋 | 기본 / 단순, 기본 선택 | 기본은 `@%#*+=-:.` 뒤에 공백, 단순은 `@*:.` 뒤에 공백. 사용자 직접 입력은 제외한다. |
| 컬러 | 끔 | 끄면 검은 글자, 켜면 축소 이미지의 픽셀 색상을 사용한다. 배경은 흰색으로 고정한다. |
| 출력 폭 | 기본 100열, 40~160열 | 값이 클수록 세밀해진다. 슬라이더 조작을 마친 시점에 재변환한다. |
| 텍스트 복사 | 버튼 | 클립보드 API를 시도하고, 로컬 파일 환경에서 사용할 수 없거나 실패하면 직접 선택·복사를 안내한다. |
| TXT 다운로드 | 버튼 | 일반 텍스트 Blob과 임시 다운로드 링크를 사용한다. 사용 후 객체 URL을 해제한다. |

밝기·대비·반전·글꼴·배경색 옵션은 추가하지 않는다. 컬러 결과의 재현도가 부족한 경우에만 후속 개선으로 검토한다.

## 5. 구현 순서 (단계별)

1. **단일 파일 뼈대:** 파일 입력, 미리보기, 옵션, 결과, 버튼과 최소 CSS를 배치한다.
2. **흑백 변환:** 이미지 로드 → 비율 보정 → Canvas 축소 → 밝기 매핑 → 텍스트 출력을 연결한다. 우선 기본 문자셋과 100열로 확인한다.
3. **최소 옵션 연결:** 문자셋·출력 폭 변경과 컬러 표시를 추가한다. 같은 변환 흐름을 재사용한다.
4. **내보내기·예외 처리:** 복사, TXT 다운로드, 빈 상태, 잘못된 파일, 용량 제한, 연속 선택, 객체 URL 해제를 마무리한다.
5. **직접 열어 검증:** 네트워크 없이 `index.html`을 열어 PNG·JPEG·투명 WebP, 가로·세로로 긴 이미지, 손상 파일을 확인한다. 흑백·컬러 전환, 출력 폭, 공백·줄바꿈 유지, 복사 실패 안내, TXT 내용 일치를 점검한다. 밝기 0과 255가 각각 첫 문자와 마지막 공백에 매핑되는 최소 실행 검증도 포함한다.

완료 기준은 파일 선택부터 변환·옵션 변경·텍스트 저장까지 로컬 브라우저에서 동작하는 것이다. 배포 설정이나 별도 프로젝트 구조는 만들지 않는다.
